# TODO — dashboard

Задачи по этой тулзе: баги, фичи, технический долг, идеи. Пишем сюда оба —
и пользователь, и Claude.

> **Правило работы со списком.** Этот файл — только хранилище задач.
> Claude не берёт отсюда ничего в работу по собственной инициативе: ни при
> «заодно поправь», ни когда задача выглядит тривиальной, ни когда она рядом
> с тем, что уже правится. Работа над пунктом начинается только по явному
> указанию пользователя на конкретный пункт.

## Баги

_Пусто._

## Фичи

_Пусто._

## Технический долг

- [ ] Подсветка зоны броска на разделе CVE не зажигается от документа, хотя
      экран уже принимает файл, брошенный мимо зоны. На разделе билдов
      `dragover` на всём документе зажигает зону — человеку заранее видно,
      что страница возьмёт файл; на CVE документный `dragover` файл берёт,
      но зону не подсвечивает, и обещание получается без внешнего знака.
      Чтобы закрыть разницу, `cve.js` нужны свои документные `dragover` (зажигает
      зону) и `dragleave` (гасит её обратно) — по образцу пары в `files.js`.
- [ ] `rail.js`: `stopPropagation`, которым чип рельса при переносе не даёт
      броску дойти до документного обработчика files.js, не покрыт тестом.
      Раньше это было непроверяемо: стаб DOM у `stopPropagation` на bubble
      был пустышкой. Теперь стаб честно останавливает всплытие, так что
      тест «бросок чипа рельса не долетает до files.js» наконец можно
      написать.

## Идеи

_Пусто._
