"""Пакет dashboard: сбор данных koji и GitLab, дашборд патчей."""
import logging

# Единственное место, где написан номер версии: CLI, снапшот и собранная
# страница берут его отсюда. Версия семантическая и описывает инструмент,
# а не формат снапшота — у формата свой номер, SCHEMA в model.py, и растут
# они порознь. Что изменилось в каждой версии, сказано в CHANGELOG.md.
__version__ = "3.5.1"

# библиотека не навязывает вывод: без настройки записи уходят в никуда
logging.getLogger(__name__).addHandler(logging.NullHandler())
