# Фильтры: три положения, группы и меню — план

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** У каждого признака появляется третье положение «нет», у группы
признаков — выбор между И и ИЛИ, а строка чипов уступает место кнопке с
меню.

**Architecture:** Набор фильтров вкладки становится картой `ключ → 1 | -1`.
Группы признаков описаны в `labels.js` — это словарь страницы, а не её
состояние. Совпадение строки считает `page.js` по группам. Кнопку и плашку
меню держит новый модуль `filters.js` — владелец узла, устроенный как
`rail.js`.

**Tech Stack:** Ванильный ES2015+ в UMD-обёртке вокруг `KP`, CSS без
препроцессора, сборка одним файлом через `dashboard/build.py`, тесты —
`node --test` поверх `tests/js/domstub.js` и `python3 -m unittest`.

## Global Constraints

- Собранная страница остаётся одним самодостаточным файлом: никаких
  внешних запросов, никаких библиотек.
- Формат снапшота (`schema: 1`) и золотая фикстура
  `tests/js/fixtures/page-data.golden.json` не трогаются.
- Ключи фильтров (`cve`, `has-patch`, `patches+`, …) остаются теми же
  строками — в ссылке, в метках строк и в плашках.
- Старая ссылка `#tab=state&f=cve,autogen` открывает тот же срез, что и
  раньше: минусов нет, `any=` нет, все группы в режиме `все`.
- Id групп (`classes`, `build`, `trouble`, `status`, `change`) уезжают в
  ссылку и потому неизменны.
- Плашки остаются органом управления: клик ставит и снимает фильтр.
- Комментарии и подписи — по-русски, в тон соседним файлам: объясняют
  «почему», а не пересказывают код.
- Номер версии не поднимается: 2.0.0 ещё не выпущена, дописывается её же
  запись в `CHANGELOG.md`.
- Ветка `feature/filters` от `develop`; в `develop` не вливать без апрува
  человека.
- После каждой задачи обе сюиты зелёные:
  `python3 -m unittest discover -s tests` и `node --test tests/js/*.test.js`.

## Раскладка файлов

| файл | роль |
|---|---|
| `dashboard/assets/js/labels.js` | + `GROUPS` и `groups(tab)` |
| `dashboard/assets/js/page.js` | трёхпозиционные фильтры, режимы групп, счётчики признаков |
| `dashboard/assets/js/hash.js` | + ключ `any=` |
| `dashboard/assets/js/filters.js` | создать: владелец кнопки и плашки меню |
| `dashboard/assets/css/filters.css` | создать: кнопка, плашка, тройной переключатель |
| `dashboard/assets/js/cards.js` | `chips()` уходит, у плашки появляется вид «нет» |
| `dashboard/assets/js/ui.js` | заводит `filters`, теряет чипы |
| `dashboard/assets/dashboard.html` | `.filterbox` в панели управления вместо `#chips` |
| `dashboard/build.py` | `filters.js` в `SCRIPTS`, `filters.css` в `STYLES` |
| `dashboard/assets/css/layout.css` | убрать `.chips`, `.chip` |
| `tests/js/{labels,page,hash,cards,ui}.test.js` | новые проверки и переезд старых |
| `README.md`, `CHANGELOG.md` | раздел про фильтры и запись 2.0.0 |

---

### Task 1: Группы признаков в `labels.js`

**Files:**
- Modify: `dashboard/assets/js/labels.js`
- Test: `tests/js/labels.test.js`

**Interfaces:**
- Consumes: `setClasses(list)` и `CLASSES` — уже есть в модуле.
- Produces: `labels.groups(tab) -> [{ id, label, keys }]`, где `tab` это
  `'state'` или `'diff'`, `keys` — массив ключей фильтров в порядке показа.
  Группа без ключей в список не попадает.

- [ ] **Step 1: Написать падающий тест**

Дописать в `tests/js/labels.test.js`:

```js
test('группы «Состояния» перечислены в порядке показа', function () {
  labels.setClasses([]);
  var ids = labels.groups('state').map(function (g) { return g.id; });
  assert.deepStrictEqual(ids, ['build', 'trouble'],
                         'группа классов пуста и в список не попадает');
});

test('живые классы становятся группой', function () {
  labels.setClasses(['CVE', 'SAST']);
  var g = labels.groups('state')[0];
  assert.strictEqual(g.id, 'classes');
  assert.strictEqual(g.label, 'классы патчей');
  assert.deepStrictEqual(g.keys, ['cve', 'sast']);
});

test('класс уходит из группы вместе со своим снапшотом', function () {
  labels.setClasses(['CVE']);
  labels.setClasses([]);
  var ids = labels.groups('state').map(function (g) { return g.id; });
  assert.strictEqual(ids.indexOf('classes'), -1);
});

test('группы «Изменений» — статус и что изменилось', function () {
  labels.setClasses(['CVE']);
  var got = labels.groups('diff');
  assert.deepStrictEqual(got.map(function (g) { return g.id; }),
                         ['status', 'change']);
  /* Классов патчей у строки диффа нет: viewmodel их туда не кладёт, и
     группа классов на этой вкладке показывала бы фильтр, под который
     не попадает ни одна строка. */
  assert.strictEqual(got.map(function (g) { return g.id; })
                        .indexOf('classes'), -1);
});

test('каждый ключ группы называется по-русски', function () {
  labels.setClasses([]);
  for (const tab of ['state', 'diff']) {
    for (const group of labels.groups(tab)) {
      for (const key of group.keys) {
        assert.notStrictEqual(labels.label(key), key,
                              'ключ ' + key + ' без подписи');
      }
    }
  }
});
```

- [ ] **Step 2: Убедиться, что тесты падают**

Запустить: `node --test tests/js/labels.test.js`
Ожидается: `labels.groups is not a function`.

- [ ] **Step 3: Добавить группы в модуль**

В `dashboard/assets/js/labels.js` после объявления `STATUS_MARKS`:

```js
  /* Группы фильтров: чем признак является, а не где он нарисован. Группа —
     это и заголовок в меню, и область действия переключателя «все / любой
     из», поэтому id уезжает в ссылку и меняться не может.

     Живут они здесь, а не в page.js, по той же причине, что и подписи:
     «к чему относится этот ключ» — вопрос словаря страницы, а не её
     состояния.

     keys: null — группа набирается из данных. Классы приходят со снапшотом
     и уходят вместе с ним, поэтому список собирается на каждый вызов, а не
     один раз при загрузке. */
  const GROUPS = {
    state: [
      { id: 'classes', label: 'классы патчей', keys: null },
      { id: 'build', label: 'свойства сборки',
        keys: ['has-patch', 'inherited', 'from-commit'] },
      { id: 'trouble', label: 'проблемы',
        keys: ['problem', 'no-patch', 'no-source', 'gitlab-error',
               'internal-error'] }
    ],
    diff: [
      { id: 'status', label: 'статус',
        keys: ['added', 'removed', 'upgraded', 'downgraded', 'unchanged'] },
      { id: 'change', label: 'что изменилось',
        keys: ['changed', 'patches+', 'patches-', 'repackaged',
               'branch-changed', 'tag-changed'] }
    ]
  };

  /* Порядок классов — тот же, что у плашек классов: classOrder начинает с
     CLASSES, и слаги здесь идут оттуда же. */
  function groups(tab) {
    const out = [];
    for (const group of (GROUPS[tab] || [])) {
      const keys = group.keys || CLASSES.map((name) => text.slug(name));
      if (keys.length) {
        out.push({ id: group.id, label: group.label, keys: keys });
      }
    }
    return out;
  }
```

В `return` модуля добавить `groups: groups`.

- [ ] **Step 4: Убедиться, что тесты проходят**

Запустить: `node --test tests/js/labels.test.js`
Ожидается: зелёный.

- [ ] **Step 5: Прогнать обе сюиты и закоммитить**

```bash
python3 -m unittest discover -s tests && node --test tests/js/*.test.js
git add dashboard/assets/js/labels.js tests/js/labels.test.js
git commit -m "Признаки фильтров разложены по группам"
```

---

### Task 2: Три положения и режимы групп в `page.js`

**Files:**
- Modify: `dashboard/assets/js/page.js`
- Test: `tests/js/page.test.js`

**Interfaces:**
- Consumes: `labels.groups(tab) -> [{ id, label, keys }]` из задачи 1.
- Produces:
  - `page.activeFilters() -> { ключ: 1 | -1 }` (было `{ ключ: 1 }`);
  - `page.filterState(key) -> 1 | -1 | 0`;
  - `page.setFilter(key, state)` — `state` это `1`, `-1` или `0`;
  - `page.toggleFilter(key)` — `есть` ↔ `неважно`, из `нет` тоже в
    `неважно`; ключ `'all'` снимает всё на вкладке;
  - `page.groupMode(id) -> 'all' | 'any'`;
  - `page.setGroupMode(id, mode)`;
  - `page.filterCounts() -> { ключ: число }` по всем строкам текущей
    вкладки, без учёта фильтров;
  - `page.hashParts().filters` — массив строк с минусом у «нет»,
    отсортированный;
  - `page.hashParts().any` — массив id групп в режиме `any`,
    отсортированный.

- [ ] **Step 1: Написать падающие тесты**

Дописать в `tests/js/page.test.js` (помощники `mk`, `snap`, `build`,
`patch` в файле уже есть — использовать их):

```js
test('«нет» оставляет строки без признака', function () {
  var p = mk([snap('os-9.1', JUL, {
    classes: ['CVE'],
    builds: [build('nginx', { patches: [patch('c.patch', 'CVE')] }),
             build('curl')]
  })]);
  p.setFilter('cve', -1);
  assert.deepStrictEqual(rows(p), ['curl']);
});

test('«есть» и «нет» на одном ключе не уживаются', function () {
  var p = mk([snap('os-9.1', JUL, {
    classes: ['CVE'],
    builds: [build('nginx', { patches: [patch('c.patch', 'CVE')] })]
  })]);
  p.setFilter('cve', 1);
  p.setFilter('cve', -1);
  assert.strictEqual(p.filterState('cve'), -1);
  assert.deepStrictEqual(rows(p), []);
});

test('два «есть» в одной группе по умолчанию требуют оба признака',
  function () {
    var p = mk([snap('os-9.1', JUL, {
      classes: ['CVE', 'SAST'],
      builds: [
        build('both', { patches: [patch('c.patch', 'CVE'),
                                  patch('s.patch', 'SAST')] }),
        build('one', { patches: [patch('c.patch', 'CVE')] })
      ]
    })]);
    p.setFilter('cve', 1);
    p.setFilter('sast', 1);
    assert.deepStrictEqual(rows(p), ['both']);
  });

test('в режиме «любой из» хватает одного признака', function () {
  var p = mk([snap('os-9.1', JUL, {
    classes: ['CVE', 'SAST'],
    builds: [
      build('both', { patches: [patch('c.patch', 'CVE'),
                                patch('s.patch', 'SAST')] }),
      build('one', { patches: [patch('c.patch', 'CVE')] }),
      build('none')
    ]
  })]);
  p.setFilter('cve', 1);
  p.setFilter('sast', 1);
  p.setGroupMode('classes', 'any');
  assert.deepStrictEqual(rows(p).sort(), ['both', 'one']);
});

test('«нет» действует и в режиме «любой из»', function () {
  /* Отрицание — не одно из условий на выбор, а запрет: строка с этим
     признаком уходит независимо от того, чем группа сложена. */
  var p = mk([snap('os-9.1', JUL, {
    classes: ['CVE', 'SAST', 'AUTOGEN'],
    builds: [
      build('clean', { patches: [patch('c.patch', 'CVE')] }),
      build('dirty', { patches: [patch('c.patch', 'CVE'),
                                 patch('a.patch', 'AUTOGEN')] })
    ]
  })]);
  p.setFilter('cve', 1);
  p.setFilter('sast', 1);
  p.setFilter('autogen', -1);
  p.setGroupMode('classes', 'any');
  assert.deepStrictEqual(rows(p), ['clean']);
});

test('группы между собой складываются по И даже в режиме «любой из»',
  function () {
    var p = mk([snap('os-9.1', JUL, {
      classes: ['CVE'],
      builds: [
        build('inh', { patches: [patch('c.patch', 'CVE')], inherited: true }),
        build('own', { patches: [patch('c.patch', 'CVE')] })
      ]
    })]);
    p.setFilter('cve', 1);
    p.setGroupMode('classes', 'any');
    p.setFilter('inherited', -1);
    assert.deepStrictEqual(rows(p), ['own']);
  });

test('клик по плашке снимает и «нет» тоже', function () {
  var p = mk([snap('os-9.1', JUL, { builds: [build('nginx')] })]);
  p.setFilter('has-patch', -1);
  p.toggleFilter('has-patch');
  assert.strictEqual(p.filterState('has-patch'), 0);
  p.toggleFilter('has-patch');
  assert.strictEqual(p.filterState('has-patch'), 1);
});

test('счётчики считают по всем строкам вкладки, а не по видимым',
  function () {
    var p = mk([snap('os-9.1', JUL, {
      classes: ['CVE'],
      builds: [build('nginx', { patches: [patch('c.patch', 'CVE')] }),
               build('curl')]
    })]);
    p.setFilter('cve', 1);
    assert.deepStrictEqual(rows(p), ['nginx']);
    var counts = p.filterCounts();
    assert.strictEqual(counts.cve, 1);
    assert.strictEqual(counts['has-patch'], 1);
    assert.strictEqual(counts.problem, 0);
  });

test('ссылка несёт минус у «нет» и режимы групп', function () {
  var p = mk([snap('os-9.1', JUL, {
    classes: ['CVE', 'SAST'], builds: [build('nginx')]
  })]);
  p.setFilter('cve', 1);
  p.setFilter('sast', -1);
  p.setGroupMode('classes', 'any');
  var parts = p.hashParts();
  assert.deepStrictEqual(parts.filters.slice().sort(), ['-sast', 'cve']);
  assert.deepStrictEqual(parts.any, ['classes']);
});

test('ссылка с минусом восстанавливается как «нет»', function () {
  var p = mk([snap('os-9.1', JUL, {
    classes: ['CVE'], builds: [build('nginx')]
  })]);
  p.restore({ tab: 'state', tag: null, pair: null,
              filters: ['-cve'], any: null, q: null, sort: null });
  assert.strictEqual(p.filterState('cve'), -1);
});

test('ссылка без any= оставляет все группы в режиме «все»', function () {
  var p = mk([snap('os-9.1', JUL, {
    classes: ['CVE'], builds: [build('nginx')]
  })]);
  p.setGroupMode('classes', 'any');
  p.restore({ tab: 'state', tag: null, pair: null,
              filters: ['cve'], any: null, q: null, sort: null });
  assert.strictEqual(p.groupMode('classes'), 'all');
});

test('мёртвый фильтр выбрасывается вместе со знаком', function () {
  var p = mk([snap('os-9.1', JUL, {
    classes: ['CVE'], builds: [build('nginx')]
  })]);
  p.setFilter('cve', -1);
  p.setFilter('нет-такого', -1);
  p.dropDeadFilters();
  assert.strictEqual(p.filterState('cve'), -1);
  assert.strictEqual(p.filterState('нет-такого'), 0);
});
```

Если помощника `rows(p)` в файле нет, добавить рядом с остальными:

```js
/* Имена видимых строк: почти каждая проверка фильтров смотрит именно на
   них, и разворачивать это в четыре строки на каждый тест незачем. */
function rows(p) {
  return p.visibleRows().map(function (item) { return item.row.name; });
}
```

- [ ] **Step 2: Убедиться, что тесты падают**

Запустить: `node --test tests/js/page.test.js`
Ожидается: `p.setFilter is not a function`.

- [ ] **Step 3: Завести режимы групп в состоянии**

В `dashboard/assets/js/page.js` в объекте `st` заменить комментарий и
строку `filters`:

```js
      /* «Изменения» открываются на изменившихся компонентах: неизменившиеся
         строки в этой таблице — шум, из-за которого не видно изменившихся.
         Фильтр обычный: он виден на кнопке фильтров и снимается как любой
         другой.

         Значение — 1 («есть») или -1 («нет»); отсутствие ключа значит
         «неважно». */
      filters: { state: {}, diff: { 'changed': 1 } },
      /* Режим группы признаков: 'any' значит «любой из», отсутствие ключа —
         «все», то есть И. Умолчание одинаковое у всех групп, поэтому
         хранить его незачем. */
      modes: { state: {}, diff: {} },
```

- [ ] **Step 4: Переписать блок фильтров**

Заменить весь участок от `function activeFilters()` до конца
`diffMatches` на:

```js
    function activeFilters() { return st.filters[st.tab]; }

    function filterState(key) {
      const at = st.filters[st.tab][key];
      return at === 1 || at === -1 ? at : 0;
    }

    function setFilter(key, state) {
      const set = st.filters[st.tab];
      if (state === 1 || state === -1) set[key] = state;
      else delete set[key];
    }

    /* Клик по плашке: «есть» ↔ «неважно». Из «нет» тоже в «неважно» —
       получить с плашки обратный срез нельзя, а уйти из него можно. */
    function toggleFilter(key) {
      if (key === 'all') { st.filters[st.tab] = {}; return; }
      setFilter(key, filterState(key) === 1 ? 0 : 1);
    }

    /* Режим любой вкладки, а не только текущей: matches судит и о той, на
       которой человека сейчас нет, — мёртвые фильтры отсеиваются сразу на
       обеих. */
    function modeOf(tab, id) {
      return st.modes[tab][id] === 'any' ? 'any' : 'all';
    }

    function groupMode(id) { return modeOf(st.tab, id); }

    function setGroupMode(id, mode) {
      if (mode === 'any') st.modes[st.tab][id] = 'any';
      else delete st.modes[st.tab][id];
    }

    /* Есть ли у строки этот признак. Два ключа считаются, остальные просто
       стоят в метках строки — и это единственное место, где такое знание
       нужно: и совпадение, и счётчики меню спрашивают отсюда. */
    function rowHas(row, key, tab) {
      if (tab === 'diff') {
        if (key === 'changed') return Boolean(row.changed);
        return row.marks.indexOf(key) !== -1;
      }
      if (key === 'has-patch') return row.patches.length > 0;
      if (key === 'problem') return row.problems.length > 0;
      return row.marks.indexOf(key) !== -1;
    }

    /* Совпадение строки с набором фильтров.

       Внутри группы: «нет» — запрет, он действует всегда; «есть»
       складываются по И или по ИЛИ, смотря какой у группы режим. Группы
       между собой всегда по И.

       Ключ, не попавший ни в одну группу, проверяется по И. Сейчас таких
       нет — группы покрывают все метки, которые ставит viewmodel, — но
       набор меток задаётся данными, и молча не применять неизвестный
       фильтр нельзя: он приехал бы из ссылки и не действовал, ничем себя
       не выдав. */
    function matches(row, tab) {
      const set = st.filters[tab];
      const groups = labels.groups(tab);
      const seen = {};
      let g, i, key, positives, hit;
      for (g = 0; g < groups.length; g++) {
        positives = 0;
        hit = false;
        for (i = 0; i < groups[g].keys.length; i++) {
          key = groups[g].keys[i];
          seen[key] = 1;
          if (!own(set, key)) continue;
          if (set[key] === -1) {
            if (rowHas(row, key, tab)) return false;
            continue;
          }
          positives += 1;
          if (rowHas(row, key, tab)) hit = true;
          else if (modeOf(tab, groups[g].id) === 'all') return false;
        }
        if (positives && !hit) return false;
      }
      for (key in set) {
        if (own(set, key) === undefined) continue;
        if (seen[key]) continue;
        if (set[key] === -1) { if (rowHas(row, key, tab)) return false; }
        else if (!rowHas(row, key, tab)) return false;
      }
      return true;
    }

    function stateMatches(row) { return matches(row, 'state'); }
    function diffMatches(row) { return matches(row, 'diff'); }

    /* Сколько строк вкладки подходит под каждый признак само по себе, без
       оглядки на другие фильтры: так же считают плашки. Зовут это при
       открытии меню, а не на каждую перерисовку — иначе лишний проход по
       всем строкам на каждый клик. */
    function filterCounts() {
      const tab = st.tab;
      const host = tab === 'diff' ? curPair() : curSnap();
      const list = host ? (tab === 'diff' ? host.rows : host.builds) : [];
      const out = {};
      for (const group of labels.groups(tab)) {
        for (const key of group.keys) {
          let n = 0;
          for (const row of list) { if (rowHas(row, key, tab)) n += 1; }
          out[key] = n;
        }
      }
      return out;
    }
```


- [ ] **Step 5: Поправить `dropDeadFilters`, `restore` и `hashParts`**

`dropDeadFilters` теряет знак, потому что собирает набор через `setFrom`.
Заменить его тело:

```js
    function dropDeadFilters() {
      const tabs = ['state', 'diff'];
      for (const tab of tabs) {
        const from = st.filters[tab], live = {};
        for (const key in from) {
          if (own(from, key) === undefined) continue;
          if (knownFilter(key, tab)) live[key] = from[key];
        }
        st.filters[tab] = live;
      }
    }
```

В `restore` заменить строку `if (filters) st.filters[st.tab] = setFrom(filters);`:

```js
      /* Минус перед ключом значит «нет». Разбирает его page, а не hash:
         hash.js о смысле ключей не знает ничего и не должен узнать. */
      if (filters) {
        const set = {};
        for (const raw of filters) {
          if (raw.charAt(0) === '-') { if (raw.length > 1) set[raw.slice(1)] = -1; }
          else if (raw) set[raw] = 1;
        }
        st.filters[st.tab] = set;
      }
      /* Ссылка без any= значит «все группы по И» — и это не то же самое,
         что «ссылка о режимах молчит»: режим, оставшийся от прошлого
         просмотра, показал бы под присланной ссылкой другой срез. */
      if (parsed.any !== null && parsed.any !== undefined) {
        const modes = {};
        for (const id of parsed.any) { if (id) modes[id] = 'any'; }
        st.modes[st.tab] = modes;
      }
```

В `hashParts` заменить строку `filters:`:

```js
        filters: keys(activeFilters()).map(
          (key) => (activeFilters()[key] === -1 ? '-' : '') + key).sort(),
        any: keys(st.modes[st.tab]).sort(),
```

- [ ] **Step 6: Отдать новое наружу**

В `return` модуля `page.js` заменить строку
`activeFilters, toggleFilter,` на:

```js
      activeFilters, filterState, setFilter, toggleFilter,
      groupMode, setGroupMode, filterCounts,
```

- [ ] **Step 7: Убедиться, что тесты проходят**

Запустить: `node --test tests/js/page.test.js`
Ожидается: зелёный.

- [ ] **Step 8: Прогнать обе сюиты**

Запустить: `python3 -m unittest discover -s tests && node --test tests/js/*.test.js`
Ожидается: `ui.test.js` пока зелёный — чипы на месте, `toggleFilter` для
плашек работает по-старому. Если падает проверка про `unchanged` и
`changed` — так и задумано: правило убрано. Такую проверку заменить на
обратную по смыслу:

```js
test('«версия та же» и «что-то изменилось» теперь пересекаются', function () {
  /* Раньше вторая плашка гасила первую. Это был костыль под клик: в меню
     оба признака видны и ставятся осознанно, а пересечение — законный
     запрос «версия не менялась, а состав поехал». */
  var p = mk([snap('os-9.1', JUL, { builds: [build('nginx')] }),
              snap('os-9.2', AUG, {
                builds: [build('nginx', { rpms: ['a.x86_64', 'b.x86_64'] })]
              })]);
  p.st.tab = 'diff';
  p.setFilter('unchanged', 1);
  p.setFilter('changed', 1);
  assert.strictEqual(p.filterState('unchanged'), 1);
  assert.strictEqual(p.filterState('changed'), 1);
  assert.deepStrictEqual(rows(p), ['nginx']);
});
```

- [ ] **Step 9: Коммит**

```bash
git add dashboard/assets/js/page.js tests/js/page.test.js
git commit -m "Фильтр стал трёхпозиционным, у группы появился режим"
```

---

### Task 3: Ключ `any=` в адресной строке

**Files:**
- Modify: `dashboard/assets/js/hash.js`
- Test: `tests/js/hash.test.js`

**Interfaces:**
- Consumes: ничего.
- Produces: `hash.parse(raw).any` — массив строк, `null`, если ключа в
  строке не было, пустой массив при `any=`; `hash.format(parts)` пишет
  `any=` только когда `parts.any` непустой.

- [ ] **Step 1: Написать падающие тесты**

Дописать в `tests/js/hash.test.js`:

```js
test('any= разбирается списком групп', function () {
  assert.deepStrictEqual(hash.parse('#tab=state&any=classes,status').any,
                         ['classes', 'status']);
});

test('без any= режимы групп из ссылки не читаются', function () {
  assert.strictEqual(hash.parse('#tab=state&f=cve').any, null);
});

test('пустой any= значит «все группы по И»', function () {
  assert.deepStrictEqual(hash.parse('#tab=state&any=').any, []);
});

test('any= пишется только когда есть что писать', function () {
  var parts = { tab: 'state', tag: null, pair: null, filters: ['cve'],
                any: [], q: '', sort: { key: 'name', asc: true } };
  assert.strictEqual(hash.format(parts).indexOf('any='), -1);
  parts.any = ['classes'];
  assert.ok(hash.format(parts).indexOf('any=classes') !== -1,
            hash.format(parts));
});

test('минус в f= проходит насквозь строкой', function () {
  /* hash.js о смысле ключей не знает: минус разбирает page.restore. */
  assert.deepStrictEqual(hash.parse('#f=cve,-autogen').filters,
                         ['cve', '-autogen']);
});
```

- [ ] **Step 2: Убедиться, что тесты падают**

Запустить: `node --test tests/js/hash.test.js`
Ожидается: `undefined !== ['classes','status']`.

- [ ] **Step 3: Добавить ключ в разбор и сборку**

В `dashboard/assets/js/hash.js` в `parse` в объекте `out` добавить поле
`any: null`, и в разбор — ветку рядом с `f`:

```js
      else if (key === 'f') out.filters = val ? val.split(',') : [];
      /* Группы, переключённые в «любой из». Отсутствие ключа и пустой
         список — разные вещи: первое значит «ссылка о режимах молчит»,
         второе — «все группы по И». */
      else if (key === 'any') out.any = val ? val.split(',') : [];
```

В `format` после строки с `f=`:

```js
    /* any= пишем, только когда есть что писать: умолчание — все группы по
       И, и пустой ключ в каждой ссылке был бы шумом. */
    if ((parts.any || []).length) {
      out.push('any=' + encodeURIComponent(parts.any.join(',')));
    }
```

- [ ] **Step 4: Убедиться, что тесты проходят**

Запустить: `node --test tests/js/hash.test.js`
Ожидается: зелёный.

- [ ] **Step 5: Прогнать обе сюиты и закоммитить**

```bash
python3 -m unittest discover -s tests && node --test tests/js/*.test.js
git add dashboard/assets/js/hash.js tests/js/hash.test.js
git commit -m "Режимы групп уезжают в ссылку ключом any="
```

---

### Task 4: Кнопка и меню

**Files:**
- Create: `dashboard/assets/js/filters.js`
- Create: `dashboard/assets/css/filters.css`
- Modify: `dashboard/assets/dashboard.html` (панель управления)
- Modify: `dashboard/build.py` (`SCRIPTS`, `STYLES`)
- Modify: `dashboard/assets/js/ui.js` (создание модуля, перерисовка)
- Test: `tests/js/ui.test.js`

**Interfaces:**
- Consumes: `page.filterState(key)`, `page.setFilter(key, state)`,
  `page.groupMode(id)`, `page.setGroupMode(id, mode)`,
  `page.filterCounts()`, `page.toggleFilter('all')`, `page.st.tab`,
  `labels.groups(tab)`, `labels.label(key)`.
- Produces: `KP.filters.create({ box, button, page, labels, text, app })
  -> { sync, close }`, где
  - `box` — узел плашки меню, `button` — кнопка;
  - `app.render()` зовётся после каждой правки фильтра;
  - `sync()` обновляет подпись кнопки и, если меню открыто, его содержимое;
  - `close()` прячет меню — её зовут при смене вкладки.

- [ ] **Step 1: Написать падающие тесты**

Дописать в `tests/js/ui.test.js`:

```js
function filterBtn(dom) { return dom.id('filters'); }

test('кнопка фильтров считает поставленные условия', function () {
  var dom = load();
  store.add([snap('os-9.1', JUL, { classes: ['CVE'],
    builds: [build('nginx', { patches: [patch('c.patch', 'CVE')] })] })],
    'a.json');
  assert.strictEqual(filterBtn(dom).textContent, 'Фильтры');
  pressCard(dom, 'tab-state', 'cve');
  assert.strictEqual(filterBtn(dom).textContent, 'Фильтры · 1');
});

test('меню открывается кнопкой и закрывается ею же', function () {
  var dom = load();
  store.add([snap('os-9.1', JUL, { builds: [build('nginx')] })], 'a.json');
  assert.strictEqual(dom.id('filtermenu').hidden, true);
  dom.fire(filterBtn(dom), 'click', {});
  assert.strictEqual(dom.id('filtermenu').hidden, false);
  assert.strictEqual(filterBtn(dom).getAttribute('aria-expanded'), 'true');
  dom.fire(filterBtn(dom), 'click', {});
  assert.strictEqual(dom.id('filtermenu').hidden, true);
});

test('Escape закрывает меню', function () {
  var dom = load();
  store.add([snap('os-9.1', JUL, { builds: [build('nginx')] })], 'a.json');
  dom.fire(filterBtn(dom), 'click', {});
  dom.fire(dom.document.body, 'keydown', { key: 'Escape' });
  assert.strictEqual(dom.id('filtermenu').hidden, true);
});

/* Кнопки меню скрипт рисует через innerHTML, а заглушка разметку из строк
   не разбирает. Ставим такую же кнопку настоящим узлом: проверяется
   обработчик, а не то, как браузер её отрисует. */
function pressMenu(dom, attr, value) {
  var node = dom.document.createElement('button');
  node.setAttribute(attr, value);
  dom.id('filtermenu').appendChild(node);
  dom.fire(node, 'click', {});
}

test('меню и плашка правят одно и то же состояние', function () {
  var dom = load();
  store.add([snap('os-9.1', JUL, { classes: ['CVE'],
    builds: [build('nginx', { patches: [patch('c.patch', 'CVE')] }),
             build('curl')] })], 'a.json');
  pressMenu(dom, 'data-fset', 'cve:-1');
  assert.strictEqual(filterBtn(dom).textContent, 'Фильтры · 1');
  assert.ok(dom.id('state-rows').innerHTML.indexOf('curl') !== -1,
            dom.id('state-rows').innerHTML);
  assert.strictEqual(dom.id('state-rows').innerHTML.indexOf('nginx'), -1,
                     'строка с CVE-патчем осталась под фильтром «нет»');
  /* Плашка того же признака снимает его в «неважно». */
  pressCard(dom, 'tab-state', 'cve');
  assert.strictEqual(filterBtn(dom).textContent, 'Фильтры');
});

test('переключатель группы уезжает в адрес', function () {
  var dom = load();
  store.add([snap('os-9.1', JUL, { classes: ['CVE'],
    builds: [build('nginx', { patches: [patch('c.patch', 'CVE')] })] })],
    'a.json');
  pressMenu(dom, 'data-fmode', 'classes:any');
  assert.ok(dom.location.hash.indexOf('any=classes') !== -1,
            dom.location.hash);
});

test('«сбросить всё» снимает фильтры вкладки', function () {
  var dom = load();
  store.add([snap('os-9.1', JUL, { classes: ['CVE'],
    builds: [build('nginx', { patches: [patch('c.patch', 'CVE')] })] })],
    'a.json');
  pressCard(dom, 'tab-state', 'cve');
  pressMenu(dom, 'data-fclear', '1');
  assert.strictEqual(filterBtn(dom).textContent, 'Фильтры');
});
```

- [ ] **Step 2: Убедиться, что тесты падают**

Запустить: `node --test tests/js/ui.test.js`
Ожидается: `в шаблоне нет id="filters"`.

- [ ] **Step 3: Поставить узлы в шаблон**

В `dashboard/assets/dashboard.html` внутри `<div class="controls" id="controls">`
перед кнопкой `#expand` добавить:

```html
      <!-- Кнопка и плашка стоят в одной обёртке: плашка позиционируется
           от неё, а вся панель управления переезжает между вкладками. -->
      <div class="filterbox" id="filterbox">
        <button type="button" id="filters" class="toggle" aria-expanded="false"
                aria-haspopup="true"
                data-tip="Что показывать в таблице: признаки строки и классы патчей">Фильтры</button>
        <div class="filtermenu" id="filtermenu" hidden></div>
      </div>
```

- [ ] **Step 4: Написать модуль**

Создать `dashboard/assets/js/filters.js`:

```js
/* Фильтры: кнопка в панели управления и плашка меню под ней. Владеет
   обоими узлами и своими слушателями; наружу отдаёт sync и close.

   Признак трёхпозиционный, и меню — единственное место, где видно все три
   положения сразу: плашка умеет только «есть» и «неважно». Отсюда же
   переключается режим группы — «все» или «любой из».

   Числа рядом с признаками считаются при открытии меню, а не на каждую
   перерисовку: это проход по всем строкам вкладки, и делать его ради
   закрытой плашки незачем. */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) {
    module.exports = factory();
  } else {
    root.KP = root.KP || {};
    root.KP.filters = factory();
  }
}(typeof globalThis !== 'undefined' ? globalThis : this, () => {
  'use strict';

  const STATES = [[0, 'неважно'], [1, 'есть'], [-1, 'нет']];

  function create(deps) {
    const box = deps.box, button = deps.button;
    const page = deps.page, labels = deps.labels, text = deps.text;
    const app = deps.app, esc = deps.text.esc;
    let open = false;

    function count() {
      return text.keys(page.activeFilters()).length;
    }

    function syncButton() {
      const n = count();
      button.textContent = n ? `Фильтры · ${n}` : 'Фильтры';
      button.className = n ? 'toggle on' : 'toggle';
    }

    function stateHtml(key) {
      return `<span class="fset" role="group" aria-label="${esc(labels.label(key))}">`
        + STATES.map(([value, name]) =>
            `<button type="button" data-fset="${esc(key)}:${value}"`
            + ` aria-pressed="${page.filterState(key) === value}">`
            + `${name}</button>`).join('')
        + '</span>';
    }

    function rowHtml(key, counts) {
      const n = text.own(counts, key);
      return '<div class="frow">'
        + `<span class="fl">${esc(labels.label(key))}</span>`
        + `<span class="fn">${esc(n === undefined ? '' : n)}</span>`
        + stateHtml(key) + '</div>';
    }

    function modeHtml(id) {
      const mode = page.groupMode(id);
      return '<span class="fmode" role="group" aria-label="как складывать">'
        + `<button type="button" data-fmode="${esc(id)}:all"`
        + ` aria-pressed="${mode === 'all'}">все</button>`
        + `<button type="button" data-fmode="${esc(id)}:any"`
        + ` aria-pressed="${mode === 'any'}">любой из</button></span>`;
    }

    function groupHtml(group, counts) {
      return '<div class="fgroup"><div class="fghead">'
        + `<span class="fgname">${esc(group.label)}</span>`
        + modeHtml(group.id) + '</div>'
        + group.keys.map((key) => rowHtml(key, counts)).join('') + '</div>';
    }

    function menuHtml() {
      const counts = page.filterCounts();
      const groups = labels.groups(page.st.tab);
      if (!groups.length) return '<div class="fnone">фильтровать нечего</div>';
      return '<div class="fhead"><span class="fl">фильтры</span>'
        + '<button type="button" class="fclear" data-fclear="1">'
        + 'сбросить всё</button></div>'
        + groups.map((group) => groupHtml(group, counts)).join('');
    }

    function draw() { box.innerHTML = menuHtml(); }

    function sync() {
      syncButton();
      if (open) draw();
    }

    function show(on) {
      open = Boolean(on);
      box.hidden = !open;
      button.setAttribute('aria-expanded', String(open));
      if (open) draw();
    }

    function close() { if (open) show(false); }

    button.addEventListener('click', (e) => {
      e.preventDefault();
      show(!open);
    });

    box.addEventListener('click', (e) => {
      let node = e.target, bits;
      while (node && node !== box) {
        if (node.getAttribute) {
          if (node.getAttribute('data-fclear')) {
            page.toggleFilter('all');
            app.render();
            return;
          }
          const set = node.getAttribute('data-fset');
          if (set) {
            bits = set.split(':');
            page.setFilter(bits[0], parseInt(bits[1], 10));
            app.render();
            return;
          }
          const mode = node.getAttribute('data-fmode');
          if (mode) {
            bits = mode.split(':');
            page.setGroupMode(bits[0], bits[1]);
            app.render();
            return;
          }
        }
        node = node.parentNode;
      }
    });

    /* Клик мимо закрывает: плашка лежит поверх таблицы, и оставлять её
       открытой, когда человек уже работает со строками, — значит закрывать
       ему то, ради чего он фильтр и ставил. Свои клики до документа
       доходят тоже, поэтому спрашиваем, откуда пришло событие. */
    document.addEventListener('click', (e) => {
      if (!open) return;
      let node = e.target;
      while (node && node !== document) {
        if (node === box || node === button) return;
        node = node.parentNode;
      }
      show(false);
    });

    document.addEventListener('keydown', (e) => {
      if (open && e.key === 'Escape') show(false);
    });

    return { sync, close };
  }

  return { create };
}));
```

- [ ] **Step 5: Написать стили**

Создать `dashboard/assets/css/filters.css`:

```css
/* Кнопка фильтров и плашка меню под ней. */

/* Обёртка нужна плашке: она позиционируется от кнопки, а не от панели
   управления, которая переезжает между вкладками. */
.filterbox { position: relative; flex: 0 0 auto; }
/* Поставленный фильтр виден и с закрытым меню: кнопка горит акцентом и
   несёт число условий. Строка чипов раньше отвечала на вопрос «почему
   строк так мало» — теперь отвечает она. */
#filters.on { border-color: var(--accent); color: var(--accent); }

/* Плашка лежит поверх таблицы: в потоке она раздвигала бы панель
   управления и уводила таблицу вниз на каждое открытие. z-index больше,
   чем у липкой шапки таблицы, но меньше, чем у сообщений и подсказки. */
.filtermenu { position: absolute; top: calc(100% + .4rem); left: 0;
  z-index: 60; width: min(23rem, calc(100vw - 2.5rem));
  max-height: 70vh; overflow-y: auto;
  padding: .6rem .7rem; border-radius: var(--r-2);
  border: 1px solid var(--line); background: var(--card);
  box-shadow: 0 6px 20px rgba(0, 0, 0, .3); }
.filtermenu[hidden] { display: none; }

.fhead { display: flex; align-items: center; gap: .5rem;
  padding-bottom: .45rem; margin-bottom: .45rem;
  border-bottom: 1px solid var(--line); }
.fhead .fl { color: var(--muted); font-size: .64rem; text-transform: uppercase;
  letter-spacing: .09em; margin-right: auto; }
.fclear { padding: .1rem .45rem; font-size: .72rem; color: var(--muted); }

.fgroup + .fgroup { margin-top: .55rem; }
.fghead { display: flex; align-items: center; gap: .5rem; margin-bottom: .2rem; }
.fgname { color: var(--muted); font-size: .64rem; text-transform: uppercase;
  letter-spacing: .09em; margin-right: auto; }

/* Строка признака: подпись тянется, число и переключатель стоят по правому
   краю столбиками — так их можно читать сверху вниз, не перескакивая. */
.frow { display: flex; align-items: center; gap: .5rem; padding: .12rem 0; }
.frow .fl { flex: 1 1 auto; font-family: var(--sans); font-size: .78rem;
  overflow-wrap: anywhere; }
.frow .fn { flex: 0 0 2.2rem; text-align: right; color: var(--muted);
  font-size: .74rem; font-variant-numeric: tabular-nums; }

/* Тройной переключатель: три кнопки встык, одна всегда нажата. Общий вид
   кнопки страницы им не годится — там пилюля с полем вокруг, а здесь ряд,
   который читается как одна деталь. */
.fset, .fmode { display: inline-flex; flex: 0 0 auto;
  border: 1px solid var(--line); border-radius: 999px; overflow: hidden; }
.fset button, .fmode button { padding: .1rem .5rem; border: 0; border-radius: 0;
  background: none; color: var(--muted); font-size: .7rem; line-height: 1.5;
  white-space: nowrap; }
.fset button + button, .fmode button + button {
  border-left: 1px solid var(--line); }
.fset button:hover, .fmode button:hover { color: var(--fg); border-color: var(--line); }
.fset button[aria-pressed=true], .fmode button[aria-pressed=true] {
  background: var(--line); color: var(--fg); }
/* «Нет» — единственное положение, которое сужает выдачу отрицанием, и
   единственное, которое стоит покрасить: иначе его не отличить от «есть»
   боковым зрением. */
.fset button[data-fset$=":-1"][aria-pressed=true] { color: var(--removed); }
.fnone { color: var(--muted); font-size: .78rem; }
```

- [ ] **Step 6: Вписать в сборку**

В `dashboard/build.py`:

```python
SCRIPTS = ("vercmp.js", "rpms.js", "diff.js", "viewmodel.js", "store.js",
           "text.js", "labels.js", "hash.js", "page.js", "markup.js",
           "tables.js", "cards.js", "filters.js", "rail.js", "files.js",
           "tips.js", "toasts.js", "ui.js")
STYLES = ("base.css", "layout.css", "rail.css", "cards.css", "table.css",
          "filters.css", "toasts.css", "tip.css")
```

- [ ] **Step 7: Связать в `ui.js`**

В UMD-обёртке `ui.js` добавить `require('./filters.js')` и
`root.KP.filters` последними доводами, а в список параметров фабрики —
`filtersmod` (после `toastsmod`).

Рядом с созданием `rail` завести модуль:

```js
  const filters = filtersmod.create({
    box: document.getElementById('filtermenu'),
    button: document.getElementById('filters'),
    page: page, labels: labels, text: text, app: app });
```

В `render()` рядом с `renderChips()` (или на её месте, если задача 5 уже
сделана) позвать `filters.sync();`.

В `showTab()` после переноса панели управления добавить `filters.close();`
— меню одной вкладки на другой показывало бы чужие признаки.

- [ ] **Step 8: Убедиться, что тесты проходят**

Запустить: `node --test tests/js/ui.test.js`
Ожидается: зелёный.

- [ ] **Step 9: Прогнать обе сюиты и закоммитить**

```bash
python3 -m unittest discover -s tests && node --test tests/js/*.test.js
git add dashboard/assets/js/filters.js dashboard/assets/css/filters.css \
        dashboard/assets/dashboard.html dashboard/build.py \
        dashboard/assets/js/ui.js tests/js/ui.test.js
git commit -m "Кнопка фильтров с меню на три положения"
```

---

### Task 5: Строка чипов уходит

**Files:**
- Modify: `dashboard/assets/js/cards.js` (убрать `chips`, добавить вид «нет»)
- Modify: `dashboard/assets/js/ui.js` (`chipsBox`, `renderChips`, `data-chip`)
- Modify: `dashboard/assets/dashboard.html` (убрать `#chips`)
- Modify: `dashboard/assets/css/layout.css` (убрать `.chips`, `.chip`)
- Modify: `dashboard/assets/css/cards.css` (вид плашки «нет»)
- Test: `tests/js/cards.test.js`, `tests/js/ui.test.js`

**Interfaces:**
- Consumes: `page.filterState(key)` из задачи 2, `filters.sync()` из задачи 4.
- Produces: `cards.chips` больше нет; у плашки в «нет» стоит класс `is-no`.

- [ ] **Step 1: Переписать проверки**

В `tests/js/cards.test.js` удалить четыре проверки `cards.chips(...)`
(строки около 86–100) целиком.

В `tests/js/ui.test.js` заменить четыре обращения
`dom.id('chips').innerHTML` на подпись кнопки. Проверки, которые ждали
пустых чипов, становятся:

```js
  assert.strictEqual(filterBtn(dom).textContent, 'Фильтры',
                     'фильтр класса из выгруженного снапшота остался живым');
```

а та, что ждала `sast` в чипах:

```js
  assert.strictEqual(filterBtn(dom).textContent, 'Фильтры · 1');
```

Добавить проверку вида «нет» на плашке:

```js
test('плашка показывает, что признак стоит в «нет»', function () {
  var dom = load();
  store.add([snap('os-9.1', JUL, { classes: ['CVE'],
    builds: [build('nginx', { patches: [patch('c.patch', 'CVE')] })] })],
    'a.json');
  pressMenu(dom, 'data-fset', 'cve:-1');
  assert.ok(dom.id('class-cards').innerHTML.indexOf('is-no') !== -1,
            dom.id('class-cards').innerHTML);
});
```

- [ ] **Step 2: Убедиться, что тесты падают**

Запустить: `node --test tests/js/ui.test.js tests/js/cards.test.js`
Ожидается: падение на `is-no`.

- [ ] **Step 3: Убрать `chips` из `cards.js`**

Удалить функцию `chips` вместе с её комментарием и убрать её из `return`.
Шапку файла поправить: «Карточки-счётчики над таблицами» — чипов у модуля
больше нет.

- [ ] **Step 4: Показать «нет» на плашке**

В `cards.js` в `cardHtml` третий довод-состояние не нужен: `aria-pressed`
и класс расставляет `ui.js` в `syncCards`. Изменить надо только его. В
`ui.js` заменить `syncCards`:

```js
  /* Плашка показывает все три положения признака: нажата — «есть», класс
     is-no — «нет», ничего — «неважно». Плашка «все» нажата, когда на
     вкладке не стоит ни одного фильтра. */
  function syncCards() {
    const host = st.tab === 'diff' ? diffSection : stateSection;
    const empty = !keys(activeFilters()).length;
    for (const node of host.querySelectorAll('.card[data-filter]')) {
      const key = node.getAttribute('data-filter');
      const state = key === 'all' ? (empty ? 1 : 0) : page.filterState(key);
      node.setAttribute('aria-pressed', String(state === 1));
      const base = String(node.className).replace(/\s*is-no\b/g, '');
      node.className = state === -1 ? `${base} is-no` : base;
    }
  }
```

- [ ] **Step 5: Убрать чипы из `ui.js` и шаблона**

В `ui.js`:
- удалить `const chipsBox = document.getElementById('chips');`;
- удалить `function renderChips() { … }` и её вызов в `render()`, оставив
  на этом месте `filters.sync();`;
- удалить строку `host.insertBefore(chipsBox, wrap);` в `showTab`;
- в делегированном обработчике удалить ветку `data-chip`:

```js
        const chip = node.getAttribute('data-chip');
        if (chip !== null && chip !== undefined) { toggleFilter(chip); return; }
```

В `dashboard/assets/dashboard.html` удалить строку
`<div class="chips" id="chips"></div>`.

- [ ] **Step 6: Убрать стили чипов**

В `dashboard/assets/css/layout.css` удалить блок от комментария
«Чипы активных фильтров…» до правила `.chip.clear:hover` включительно.

В `dashboard/assets/css/cards.css` добавить вид «нет»:

```css
/* Признак, поставленный в «нет»: плашка приглушена и обведена цветом
   убранного. Нажатой она быть не может — «нет» это не выбор этой плашки,
   а запрет, и показывать его тем же, чем показан выбор, значило бы
   называть разные вещи одним. */
.card.is-no { border-color: var(--removed); opacity: .65; }
```

- [ ] **Step 7: Убедиться, что тесты проходят**

Запустить: `node --test tests/js/*.test.js`
Ожидается: зелёный.

- [ ] **Step 8: Прогнать обе сюиты и закоммитить**

```bash
python3 -m unittest discover -s tests && node --test tests/js/*.test.js
git add dashboard/assets/js/cards.js dashboard/assets/js/ui.js \
        dashboard/assets/dashboard.html dashboard/assets/css/layout.css \
        dashboard/assets/css/cards.css tests/js/cards.test.js \
        tests/js/ui.test.js
git commit -m "Строка чипов уступила место кнопке фильтров"
```

---

### Task 6: Проверить вёрстку и записать в документацию

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`

**Interfaces:**
- Consumes: собранную страницу из задач 1–5.
- Produces: ничего кодового.

- [ ] **Step 1: Собрать страницу и открыть меню**

```bash
S=/tmp/claude-1000/-home-foxtermore----------repo-dashboard/31b29e70-25a1-4959-b47f-d81edae82126/scratchpad
python3 -m dashboard page -o $S/dash.html
```

Дописать в конец собранного файла пробу, которая грузит фикстуры и
открывает меню, и снять снимок:

```bash
MOZ_HEADLESS=1 firefox --window-size=1400,900 \
  --screenshot $S/filters.png file://$S/probe.html
```

Проба должна: положить `tests/fixtures/rich-old.json` через
`KP.store.add`, снять анимации правилом
`* { animation: none !important; transition: none !important; }` (снимок
делается сразу после загрузки и иначе застаёт страницу на полпути) и
нажать `document.getElementById('filters').click()`.

Смотрим глазами: плашка не выходит за правый край, не залезает под липкую
шапку таблицы, тройной переключатель не переносится.

- [ ] **Step 2: Снять снимок на узком окне**

```bash
MOZ_HEADLESS=1 firefox --window-size=420,760 \
  --screenshot $S/filters-narrow.png file://$S/probe.html
```

Ожидается: плашка ужимается до ширины окна минус отступы, горизонтальной
прокрутки у страницы нет.

- [ ] **Step 3: Переписать раздел README про фильтры**

Найти абзацы про фильтры и чипы (искать «чип» и «фильтр» в README) и
заменить описанием новой системы: три положения у признака, группы с
переключателем «все / любой из», кнопка с числом условий, плашки
по-прежнему ставят и снимают. Отдельно назвать формат ссылки:

```markdown
Фильтры в ссылке: `f=cve,-autogen` — минус перед ключом значит «нет»,
`any=classes` — список групп, переключённых в «любой из». Ссылка без
`any=` открывает все группы в режиме «все», как и раньше.
```

- [ ] **Step 4: Дописать таблицы скриптов и стилей**

В README в таблице скриптов добавить строку `filters.js` («кнопка фильтров
и меню под ней») в раздел про владельцев участков DOM, в таблице стилей —
`filters.css` («кнопка фильтров, плашка меню, тройной переключатель»).
Числа «Семнадцать файлов в `assets/js/`» и «Семь файлов в `assets/css/`»
поднять на единицу. Порядок строк в таблицах должен совпадать с порядком в
`SCRIPTS` и `STYLES`.

- [ ] **Step 5: Дописать CHANGELOG**

В запись 2.0.0 добавить абзац:

```markdown
**Фильтры стали трёхпозиционными.** У каждого признака теперь три
положения — «неважно», «есть» и «нет», — а у группы признаков
переключатель «все / любой из». Запрос «есть CVE-патч и при этом нет
autogen-патча» задаётся двумя кликами. Строка чипов уступила место кнопке
«Фильтры» с числом поставленных условий и меню под ней; плашки
по-прежнему ставят и снимают фильтр. В ссылке минус перед ключом значит
«нет», а `any=` перечисляет группы, сложенные по ИЛИ; ссылки без минусов и
без `any=` открывают тот же срез, что и раньше. Заодно убрано правило, по
которому «версия та же» молча гасила «что-то изменилось»: теперь они
пересекаются, и это законный запрос — версия не менялась, а состав поехал.
```

- [ ] **Step 6: Прогнать обе сюиты и закоммитить**

```bash
python3 -m unittest discover -s tests && node --test tests/js/*.test.js
git add README.md CHANGELOG.md
git commit -m "README и CHANGELOG про новые фильтры"
```

---

## Что остаётся человеку

Ветка `feature/filters` не вливается в `develop` без апрува. После апрува:

```bash
git checkout develop
git merge --no-ff feature/filters
git push origin develop
git branch -d feature/filters
```
